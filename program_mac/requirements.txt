live2d-py>=0.3.0
PyQt5>=5.15.0
torch>=2.0.0
transformers>=5.2.0
accelerate>=1.8.0
SpeechRecognition>=3.10.0
pyaudio>=0.2.11
edge-tts>=6.1.0
pygame>=2.0.0
langdetect>=1.1.6
tiktoken>=0.8.0
requests>=2.31.0
numpy<=1.26.4
huggingface_hub>=0.16.0
chromadb>=0.4.0
sentence-transformers>=2.2.0
scikit-learn>=1.0.0

# Optional / platform-specific (not used on typical macOS installs):
# bitsandbytes — only for NVIDIA CUDA 4-bit loading; omit on Mac
# turboquant — CUDA-only KV cache; omit on Mac (use LIVEBOT_TURBOQUANT_BITS=0)
